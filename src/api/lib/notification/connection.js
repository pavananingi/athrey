var admin = require("firebase-admin");

// var serviceAccount = require("../../../config/firbase.config.json");


// admin.initializeApp({
//     credential: admin.credential.cert(serviceAccount.auth),
// })

module.exports.connection = admin